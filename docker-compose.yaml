# Skemex UI (Angular SSR). Requires external network (shared with API):
#   docker network create skemex-network
#
# Build args API_URL / TOKEN_STORAGE_ENCRYPTION_KEY come from the shell (deploy pipeline secrets).

services:
  ui-ssr:
    build:
      context: .
      dockerfile: Dockerfile
      target: ssr
      args:
        API_URL: ${API_URL}
        TOKEN_STORAGE_ENCRYPTION_KEY: ${TOKEN_STORAGE_ENCRYPTION_KEY}
    restart: unless-stopped
    expose:
      - "4000"
    environment:
      PORT: "4000"
      NODE_ENV: production
    networks:
      - skemex
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:4000').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  ui:
    build:
      context: .
      dockerfile: Dockerfile
      target: web
    restart: unless-stopped
    depends_on:
      ui-ssr:
        condition: service_healthy
    ports:
      - "4200:80"
    networks:
      - skemex

networks:
  skemex:
    name: skemex-network
    external: true
